#!/usr/bin/env bash
# Copy this file to appstore.env (gitignored) and set the production App Clip URL.
# App Store Connect scripts read this file directly; it can also be sourced.

export ZW_APPCLIP_INVOCATION_URL="https://api.example.com/app/g"
