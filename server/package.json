{
  "name": "zerozerowidget-server",
  "version": "0.1.0",
  "private": true,
  "description": "00Widget backend — Cloudflare Worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1",
    "wrangler": "^4.85.0"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
